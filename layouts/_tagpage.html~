---
---
<section class="content">
    <h2>Articles with <%= tag.capitalize%></h2>
    <ul class="archlist">
    <% items_with_tag(tag).each do |post| %>
        <li>
            <a href="<%= post.path %>"><%= post[:title]%></a>
            <span class="date"><%= attribute_to_time(post[:created_at]).strftime('%B %-d') %></span>
        </li>
    <% end %>
    </ol>
  <% end %>
</section>

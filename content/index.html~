---
title: Home
---

<section class="content">
  <ul class="listing">
    <% sorted_articles.each do |post| %>
      <li>
        <span class="date"><%= get_pretty_date(post) %></span> <a class="title" href="<%= post.path %>"><%= post[:title]%></a>
        <section class="snippet">
          <%= get_post_start(post) %>
          <span class="tags"><%= tags_for(@item, { :base_url => '/tags/' }) %></span>
        </section>
      </li>
    <% end %>
  </ul>
</section>
